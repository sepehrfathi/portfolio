<div dir="rtl">

# راه‌اندازی سایت روی هاست

فقط یک راه دارد و کارِ سختی هم ندارد: **زیپ را آپلود کن، Extract بزن، تمام.**
نه FTP لازم است، نه FileZilla، نه تنظیم Binary و Ascii.

---

## نکتهٔ مهم قبل از شروع

**همه‌چیز داخل خودِ زیپ است.** عکس‌ها، فونت‌ها، آیکن‌ها، لوگوها، کتابخانهٔ سه‌بعدی — همه لوکال‌اند.
سایت حتی یک فایل هم از اینترنت بیرون نمی‌گیرد: نه Google Fonts، نه CDN، نه هیچی.
یعنی روی شبکهٔ ملی هم کامل و سریع بالا می‌آید و هیچ‌وقت «فونت لود نشد» نمی‌گیری.

---

## مرحله ۱ — آپلود (۲ دقیقه)

۱. وارد **cPanel** شو.
۲. روی **File Manager** کلیک کن.
۳. برو داخل پوشهٔ سایت:
   - اگر دامنهٔ اصلی است → `public_html`
   - اگر ساب‌دامنهٔ `sep.onwebs.ir` است → معمولاً `public_html/sep`
     (اگر مطمئن نیستی: cPanel ← **Subdomains** ← ستون Document Root را نگاه کن)
۴. بالای صفحه **Upload** را بزن.
۵. فایل `sepehr-portfolio.zip` را انتخاب کن و صبر کن تا ۱۰۰٪ شود.
۶. برگرد به File Manager (لینک Go Back پایین صفحه).
۷. روی `sepehr-portfolio.zip` **راست‌کلیک** کن ← **Extract** ← دکمهٔ **Extract Files**.
۸. حالا روی همان فایل زیپ راست‌کلیک ← **Delete** (دیگر لازم نیست).

تمام. سایت بالاست. برو `https://sep.onwebs.ir` را باز کن.

> اگر بعد از Extract دیدی همهٔ فایل‌ها داخل یک پوشهٔ اضافه به اسم `sepehr-portfolio` رفته‌اند:
> وارد آن پوشه شو، همه را انتخاب کن (Select All)، **Move** بزن و مسیر را یک پله بالاتر بده.
> `index.html` باید مستقیم در پوشهٔ سایت باشد، نه داخل یک زیرپوشه.

---

## مرحله ۲ — فرم تماس و SMTP

فرم تماس از قبل با SMTP امن `mail.onwebs.ir:465` و ایمیل فرستندهٔ
`ceo@onwebs.ir` تنظیم و با موفقیت آزمایش شده است. پیام‌ها به
`ftsepi@gmail.com` می‌رسند و پاسخ‌دادن به هر پیام، ایمیل همان بازدیدکننده را
در قسمت Reply-To می‌گذارد.

فایل مخفی `.smtp-config.php` را همراه بقیهٔ فایل‌ها آپلود کن اما آن را حذف،
منتشر یا برای دیگران ارسال نکن. دسترسی مستقیم وب به این فایل در `.htaccess`
بسته شده است. در File Manager برای دیدنش باید **Show Hidden Files** روشن باشد.

---

## مرحله ۳ — فعال‌کردن HTTPS (۱ دقیقه)

۱. cPanel ← **SSL/TLS Status**
۲. دامنه را تیک بزن ← **Run AutoSSL**
۳. چند دقیقه صبر کن تا گواهی صادر شود.

بعد از اینکه `https://sep.onwebs.ir` بدون خطا باز شد، اگر خواستی هرکس با `http` آمد
خودکار به `https` برود: در File Manager فایل `.htaccess` را Edit کن و `#` را از ابتدای
این چهار خط بردار:

```apache
RewriteEngine On
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
```

> اگر `.htaccess` را نمی‌بینی: بالا سمت راست **Settings** ← تیک **Show Hidden Files** را بزن.
> این کار را حتماً **بعد** از فعال‌شدن SSL انجام بده، وگرنه سایت لوپ می‌خورد.

---

## زبان پیش‌فرض

از قبل روی فارسی تنظیم شده است. هر بازدیدکننده‌ای که `sep.onwebs.ir` را باز کند،
مستقیماً به `/fa/` می‌رود؛ زبان مرورگر کاربر این رفتار را تغییر نمی‌دهد.
نسخه‌های انگلیسی، عربی، چینی، روسی، اسپانیایی و نروژی همچنان از منوی زبان در دسترس‌اند.

---

## مرحله ۴ — تست

- `https://sep.onwebs.ir` باز شود ✔
- باز کردن `sep.onwebs.ir` مستقیم نسخهٔ فارسی را بیاورد ✔
- سوییچ زبان بالا کار کند ✔
- دکمهٔ دانلود رزومه پنجرهٔ پرینت بیاورد ✔
- فرم پایین صفحه را پر کن و بفرست ← باید بنویسد «پیام ارسال شد» و ایمیل به `ftsepi@gmail.com` برسد ✔

اگر ایمیل نیامد، **اول پوشهٔ Spam را نگاه کن.**

---

## سئو بعد از بالا آمدن سایت

۱. **Google Search Console** ← Add property ← `https://sep.onwebs.ir/`
۲. بخش **Sitemaps** ← این آدرس را ثبت کن:

```
https://sep.onwebs.ir/sitemap.xml
```

۳. بخش **URL Inspection** ← آدرس `https://sep.onwebs.ir/fa/` را بزن ← **Request Indexing**
۴. لینک `https://sep.onwebs.ir` را در بیو لینکدین، گیت‌هاب و سایر پروفایل‌های حرفه‌ای قرار بده.
   برای اینکه گوگل و ChatGPT بفهمند این صفحه واقعاً مالِ توست، این مهم‌ترین کار است.

---

## عوض‌کردن متن‌ها در آینده

نسخهٔ نهایی هر زبان داخل پوشهٔ همان زبان است. برای اجرای دوبارهٔ تنظیمات
مشترک، متادیتا و محتوای حرفه‌ای سایت:

```bash
python3 _src/build.py
```

هر ۷ زبان به‌روزرسانی می‌شود. بعد فقط فایل‌ها و پوشه‌های تغییرکرده را در
File Manager آپلود کن.

---

## اگر مشکلی پیش آمد

| مشکل | راه‌حل |
|---|---|
| صفحه سفید است | `index.html` داخل یک زیرپوشه افتاده؛ فایل‌ها را یک پله بیاور بالا (بالای همین صفحه توضیح داده شد) |
| فرم خطای ۵۰۰ می‌دهد | cPanel ← **Select PHP Version** ← روی ۸٫۰ یا بالاتر بگذار |
| فرم به‌جای ارسال، فایل دانلود می‌کند | PHP روی هاست فعال نیست؛ به پشتیبانی هاست بگو فعالش کنند |
| ایمیل فرم نمی‌آید | پوشهٔ Spam را بررسی کن و مطمئن شو فایل مخفی `.smtp-config.php` نیز همراه بسته Extract شده است |
| تغییرات را نمی‌بینم | کش هاست (LiteSpeed Cache) و کش مرورگر را پاک کن |
| گوی‌های سه‌بعدی نمی‌چرخند | مرورگر آن دستگاه WebGL ندارد؛ خودکار نسخهٔ ساده نشان داده می‌شود، مشکلی نیست |

</div>
